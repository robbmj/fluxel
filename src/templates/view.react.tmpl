
var React = require('react');

var <<NAME>>Store = require('../stores/<<NAME>>Store');
var <<NAME>>Actions = require('../actions/<<NAME>>Actions');

export default React.createClass({
	render () {
		return <h1><<NAME>> View</h1>
	}
});
